// 4. Поиск максимального элемента в массиве
// Рекурсивно найдите наибольший элемент в массиве.Например,
//  для массива[1, 5, 3, 9, 2] результат — 9.;

// function maxNum(arr) {
//   if (arr.lenght === 0) {
//     return arr[0];
//   }
//   const arr2 = maxNum(arr.slice(1));
//   return Math.max(arr[0], arr2);
// }
// maxNum([1, 5, 3, 9, 2]);
// const arr = [1, 5, 3, 9, 2];

// function maxNum(arr) {
//   if (arr.lenght === 0) {
//     return arr[0];
//   }
//   const arr2 = (...arr);
//   return Math.max(arr2);
// }
// maxNum(arr);